Backend:
1. Powershell 열어서 가상환경 진입
2. Backend 디렉토리에서 
pip install fastapi uvicorn 
pip install Pillow
pip install opencv-python
pip install torch torchvision timm
pip install streamlit
pip install ultralytics
3. Backend 디렉토리에서 python main.py 실행

Frontend:
원도우즈 파일 탐색기에서
프로젝트 디렉토리>fronend>index.html 우클릭 후 크롬 브라우져 연결

